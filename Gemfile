# Gemfile
source "https://rubygems.org"
# Specify the Ruby version you're using (optional but recommended)
ruby "3.3.5"

gem "kramdown-parser-gfm"
# Use Jekyll as the site generator
gem "jekyll", "~> 3.0"

# Use Bundler to manage the dependencies
gem "bundler", "~> 2.0"

# Add Jekyll plugins here (optional)
gem "jekyll-feed", "~> 0.15" # Example: Adds RSS feed generation support
gem "jekyll-seo-tag", "~> 2.7" # Example: Adds SEO tags to your site

# Optional: Add jekyll-livereload for live reloading during development
gem "jekyll-livereload", "~> 0.2.0"